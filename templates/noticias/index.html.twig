{# {% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'src/Controller/NoticiasController.php'|file_link(0) }}">src/Controller/NoticiasController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'templates/noticias/index.html.twig'|file_link(0) }}">templates/noticias/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %} #}

{% block contenido %}
    <h2>{{ titulo }} - <small>{{ fecha }}</small></h2>
    bienvenidos a las noticias de la symtienda -
    <small>{ controller_name }</small>
    <hr>
    <p>la ultima visita fue a las -> {{ ultimaVisita|date('d/m/Y') }}</p>
    <hr>

    {% for noticia in noticias %}
        <article>
            <header>
                <h3>{{ noticia.titulo }}</h3>
            </header>
            <section>{{ noticia.descripcion }}</section>
            <br>
            <footer>
                {{ noticia.fecha|date('d/m/Y') }} - 
                Author: {{ noticia.autor }}
            </footer>
        </article>
        <hr>
    {% endfor %}
{% endblock %}